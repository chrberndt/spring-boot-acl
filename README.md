# spring-boot-acl

Explore and demonstrate how to use Spring Boot's Domain Object Security (ACLs)

## Prerequisites

`docker` and `docker-compose` installed and running in root-less mode

```bash
sudo apt install docker-compose
```

## Running

```bash
./gradlew bootRun
```

## Testing

```bash
./gradlew test
```
